{
  "name": "gerenciamento_almoxarifado",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development nodemon --watch './**/*.ts' --exec ts-node ./src/index.ts",
    "build": "tsc",
    "typeorm": "typeorm-ts-node-commonjs",
    "migration:generate": "NODE_ENV=development npm run typeorm migration:generate -- -d ./src/config/database.ts",
    "migration:run": "NODE_ENV=development npm run typeorm migration:run -- -d ./src/config/database.ts",
    "migration:revert": "NODE_ENV=development npm run typeorm migration:revert -- -d ./src/config/database.ts"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/pg": "^8.16.0",
    "nodemon": "^3.1.11"
  },
  "dependencies": {
    "express": "^5.2.1",
    "joi": "^18.0.2",
    "pg": "^8.17.1",
    "ts-node": "^10.9.2",
    "typeorm": "^0.3.28",
    "typescript": "^5.9.3"
  }
}
